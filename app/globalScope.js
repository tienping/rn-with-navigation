import { API_URL } from 'react-native-dotenv';

// default alue for globalScope
export const globalScope = {
    token: '',
    tabBarColor: '',
    isAdmin: false,
    debuggerMode: false,
    api: API_URL,
    defaultAPI: 'https://api.hermo.my',
    isConnected: true,
};
